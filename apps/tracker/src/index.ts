import createEventTracker from "./tracker";

// Export types
export type * from "./types";

// Export the factory function as default
export default createEventTracker;

// Also export as named export
export { createEventTracker };
