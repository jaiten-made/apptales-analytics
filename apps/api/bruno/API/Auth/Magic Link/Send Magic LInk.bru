meta {
  name: Send Magic LInk
  type: http
  seq: 1
}

post {
  url: http://localhost:3001/auth/magic-link
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{email}}"
  }
}

vars:pre-request {
  email: john.doe@gmail.com
}

settings {
  encodeUrl: true
  timeout: 0
}
