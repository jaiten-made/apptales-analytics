meta {
  name: Verify Magic Link
  type: http
  seq: 2
}

get {
  url: http://localhost:3001/auth/magic-link/verify?token={{sessionToken}}
  body: none
  auth: inherit
}

params:query {
  token: {{sessionToken}}
}

settings {
  encodeUrl: true
}
