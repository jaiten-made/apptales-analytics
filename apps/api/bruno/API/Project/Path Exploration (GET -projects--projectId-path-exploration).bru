meta {
  name: Path Exploration (GET /projects/:projectId/path-exploration)
  type: http
  seq: 5
}

get {
  url: http://localhost:3001/projects/:projectId/path-exploration
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
}

headers {
  Authorization: Bearer {{sessionToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
