meta {
  name: Create Event - Page View (POST /events)
  type: http
  seq: 2
}

post {
  url: http://localhost:3001/events
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sessionId": "{{sessionId}}",
    "type": "page_view",
    "properties": {
      "location": {
        "pathname": "/home"
      }
    }
  }
}

settings {
  encodeUrl: true
}
