meta {
  name: List Events (GET /events)
  type: http
  seq: 1
}

get {
  url: http://localhost:3001/events?projectId={{projectId}}
  body: none
  auth: inherit
}

params:query {
  projectId: {{projectId}}
}

headers {
  Authorization: Bearer {{sessionToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
