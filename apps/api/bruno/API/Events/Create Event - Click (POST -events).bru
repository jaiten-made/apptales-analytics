meta {
  name: Create Event - Click (POST /events)
  type: http
  seq: 3
}

post {
  url: http://localhost:3001/events
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sessionId": "{{sessionId}}",
    "type": "click"
  }
}

settings {
  encodeUrl: true
}
