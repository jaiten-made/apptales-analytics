meta {
  name: Create Session
  type: http
  seq: 3
}

post {
  url: http://localhost:3001/sessions
  body: json
  auth: inherit
}

body:json {
  {
    "projectId": {{projectId}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
